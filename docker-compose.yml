services:
  web:
    build: .
    container_name: si-ruangan-web
    ports:
      - "8085:80"
    restart: unless-stopped
    # Future services can be added here, e.g.:
    # backend:
    #   build: ./backend
    #   environment:
    #     - DATABASE_URL=postgresql://...
    #   depends_on:
    #     - db
    #
    # db:
    #   image: postgres:16-alpine
    #   environment:
    #     POSTGRES_DB: siruangan
    #     POSTGRES_USER: siruangan
    #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    #   volumes:
    #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
